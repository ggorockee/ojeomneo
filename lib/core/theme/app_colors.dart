import 'package:flutter/material.dart';

class AppColors {
  // Primary Color (오렌지 계열 - Figma 디자인)
  static const primary = Color(0xFFFFA451);
  static const primaryForeground = Color(0xFFFFFFFF);
  static const primaryLight = Color(0xFFFFF2E7); // 연한 오렌지 배경

  // Secondary (보조 색상)
  static const secondary = Color(0xFFF08626); // 진한 오렌지
  static const secondaryLight = Color(0xFFFFB974); // 밝은 오렌지

  // Background
  static const background = Color(0xFFFFFFFF);
  static const foreground = Color(0xFF1A1A1A);

  // Card
  static const card = Color(0xFFFFFFFF);
  static const cardForeground = Color(0xFF1A1A1A);

  // Muted
  static const muted = Color(0xFFF5F5F5);
  static const mutedForeground = Color(0xFF757575);

  // Border
  static const border = Color(0xFFE0E0E0);

  // Accent Colors
  static const accent = Color(0xFFFFA451); // primary와 동일
  static const accentForeground = Color(0xFFFFFFFF);

  // Destructive
  static const destructive = Color(0xFFDC2626);
  static const destructiveForeground = Color(0xFFFFFFFF);

  // Success (초록 계열)
  static const success = Color(0xFF22A45D);
  static const successForeground = Color(0xFFFFFFFF);

  // Warning
  static const warning = Color(0xFFFFA726);
  static const warningForeground = Color(0xFF1A1A1A);
}
